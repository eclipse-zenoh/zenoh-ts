{
  "name": "zenoh-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "preview": "vite preview",
    "clean": "rm -rf ./node_modules ./dist ./esm",
    "run_example": "deno run -A --no-prompt" 
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@eclipse-zenoh/zenoh-ts": "file:..",
    "eslint": "^9.10.0",
    "tslog": "^4.9.3"
  }
}
