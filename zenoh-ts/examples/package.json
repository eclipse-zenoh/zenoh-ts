{
  "name": "zenoh-ts-examples",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf ./node_modules ./dist ./esm",
    "example": "deno run -A --no-prompt" 
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@eclipse-zenoh/zenoh-ts": "file:.."
  }
}
